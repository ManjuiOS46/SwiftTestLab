.build/
.swiftpm/
SwiftTestLab.app/
.DS_Store
*.xcodeproj

# Where the app archives every generated test, pass or fail. Local output, not
# source — the folder stays on disk, it just isn't part of the repo.
SwiftTestLabTests/
